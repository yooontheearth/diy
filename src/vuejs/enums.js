export const ToInch = 0.393701;
export const ToMeter = 1000;
export const ToOriginalFromMeter = 0.001;

export const EditMode = {
    Mesh:1,
    Vertex:2
};

// MEMO : Even if the display mode is in inch, size data is held in millimeter
export const LengthDisplayMode = {
    Millimeter:1,
    Inch:2
}